# Redis (opsional - jika tidak diset atau Redis tidak tersedia, caching di-skip)
# Lokal (dengan Redis): REDIS_URL=redis://localhost:6379
# Docker Compose:       sudah di-set otomatis di docker-compose.yaml
# REDIS_URL=redis://localhost:6379
